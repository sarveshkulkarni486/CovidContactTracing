<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.spam.maddy.covidcontacttracing">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CovidContactTracing"
        tools:replace="android:icon">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".VaccineCenterDetails" />
        <activity android:name=".AddVaccineCenterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CovidHelpActivity" />
        <activity android:name=".RegisterationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>